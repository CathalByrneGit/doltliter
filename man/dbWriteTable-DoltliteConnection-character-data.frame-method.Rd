% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Doltlite-connection.R
\name{dbWriteTable,DoltliteConnection,character,data.frame-method}
\alias{dbWriteTable,DoltliteConnection,character,data.frame-method}
\alias{dbCreateTable,DoltliteConnection,character-method}
\alias{dbAppendTable,DoltliteConnection,character,data.frame-method}
\alias{dbWriteTable,DoltliteConnection,SQL,data.frame-method}
\alias{dbCreateTable,DoltliteConnection,SQL-method}
\alias{dbAppendTable,DoltliteConnection,SQL,data.frame-method}
\title{Write a data frame to a table}
\usage{
\S4method{dbWriteTable}{DoltliteConnection,character,data.frame}(
  conn,
  name,
  value,
  ...,
  row.names = FALSE,
  overwrite = FALSE,
  append = FALSE,
  field.types = NULL,
  temporary = FALSE
)

\S4method{dbCreateTable}{DoltliteConnection,character}(
  conn,
  name,
  fields,
  ...,
  field.types = NULL,
  row.names = NULL,
  temporary = FALSE
)

\S4method{dbAppendTable}{DoltliteConnection,character,data.frame}(conn, name, value, ..., row.names = NULL)

\S4method{dbWriteTable}{DoltliteConnection,SQL,data.frame}(conn, name, value, ...)

\S4method{dbCreateTable}{DoltliteConnection,SQL}(conn, name, fields, ..., row.names = NULL, temporary = FALSE)

\S4method{dbAppendTable}{DoltliteConnection,SQL,data.frame}(conn, name, value, ..., row.names = NULL)
}
\arguments{
\item{conn}{a `DoltliteConnection`.}

\item{name}{the table name.}

\item{value}{a data frame.}

\item{...}{unused.}

\item{row.names}{how to treat row names; see [DBI::sqlRownamesToColumn()].}

\item{overwrite}{whether to drop an existing table first.}

\item{append}{whether to append to an existing table.}

\item{field.types}{a named character vector of column types.}

\item{temporary}{whether to create a `TEMPORARY` table.}

\item{fields}{a data frame or named character vector defining the columns.}
}
\description{
Behaves as it does for any DBI backend. Two DoltLite-specific points are
worth knowing:
}
\details{
* The default `CREATE TABLE` declares no primary key, so the table keeps an
  ordinary `rowid` and behaves exactly as it would under SQLite.
* If you use `field.types` to declare a **non-`INTEGER` primary key**, the
  table becomes clustered on that key. It then has no `rowid` column at all
  (as with SQLite's `WITHOUT ROWID`), and the key columns are `NOT NULL`.
  Writing `NULL` into them fails. An `INTEGER PRIMARY KEY` is unaffected --
  it stays a writable `rowid` alias.

Writing a table changes the working set only. Call [dolt_commit()] to record
it in the version history.
}
