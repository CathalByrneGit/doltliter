% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dolt-branch.R
\name{dolt_active_branch}
\alias{dolt_active_branch}
\alias{active_branch}
\title{The current branch}
\usage{
dolt_active_branch(con)

active_branch(con)
}
\arguments{
\item{con}{a `DoltliteConnection`.}
}
\value{
The branch name, or `NA` when the connection is on a detached
  revision (opened at a tag, commit hash or ancestor spec), which is
  read-only.
}
\description{
The current branch
}
\examples{
con <- DBI::dbConnect(doltliter::Doltlite(), tempfile())
dolt_active_branch(con)
DBI::dbDisconnect(con)
}
