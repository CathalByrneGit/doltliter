% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dolt-branch.R
\name{dolt_branch}
\alias{dolt_branch}
\alias{dolt_branches}
\title{Create, list or delete branches}
\usage{
dolt_branch(con, name = NULL, delete = FALSE, force = FALSE)

dolt_branches(con)
}
\arguments{
\item{con}{a `DoltliteConnection`.}

\item{name}{the branch name. Omit to list branches instead.}

\item{delete}{delete `name` rather than creating it.}

\item{force}{overwrite an existing branch.}
}
\value{
When listing, a data frame of branches. Otherwise invisibly `TRUE`.
}
\description{
Create, list or delete branches
}
\examples{
con <- DBI::dbConnect(doltliter::Doltlite(), tempfile())
dolt_config(con, user.name = "Ada", user.email = "ada@example.com")
DBI::dbWriteTable(con, "t", data.frame(x = 1))
dolt_commit(con, "init")
dolt_branch(con, "experiment")
dolt_branch(con)$name
DBI::dbDisconnect(con)
}
