% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dolt-commit.R
\name{dolt_commit}
\alias{dolt_commit}
\title{Commit the current working set}
\usage{
dolt_commit(
  con,
  message,
  author = NULL,
  all = TRUE,
  allow_empty = FALSE,
  force = FALSE
)
}
\arguments{
\item{con}{a `DoltliteConnection`.}

\item{message}{the commit message.}

\item{author}{optionally `"Name <email>"`, overriding [dolt_config()] for
this commit only.}

\item{all}{stage every changed table first, like `git commit -a`
(the default).}

\item{allow_empty}{create a commit even when nothing changed.}

\item{force}{commit despite outstanding constraint violations.}
}
\value{
The new commit hash, invisibly.
}
\description{
Records a new commit in the version history. This is the Dolt sense of
"commit" and has nothing to do with [DBI::dbCommit()], which ends a SQL
transaction; see [doltliter-transactions].
}
\examples{
con <- DBI::dbConnect(doltliter::Doltlite(), tempfile())
dolt_config(con, user.name = "Ada", user.email = "ada@example.com")
DBI::dbWriteTable(con, "users", data.frame(id = 1:2, nm = c("a", "b")))
dolt_commit(con, "Initial load")
dolt_log(con)$message
DBI::dbDisconnect(con)
}
