% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dolt-sql.R
\name{dolt_scalar}
\alias{dolt_scalar}
\title{Call a Dolt scalar SQL function}
\usage{
dolt_scalar(con, fn, ...)
}
\arguments{
\item{con}{a `DoltliteConnection`.}

\item{fn}{the function name, e.g. `"dolt_commit"`.}

\item{...}{arguments, passed through in order.}
}
\value{
The function's single return value.
}
\description{
Escape hatch for a version-control function this package does not wrap
yet. Arguments are bound as parameters, so they are always treated as
values.
}
\examples{
con <- DBI::dbConnect(doltliter::Doltlite(), tempfile())
dolt_scalar(con, "dolt_config", "user.name", "Ada")
DBI::dbDisconnect(con)
}
