# Garbage-collect unreachable chunks

Garbage-collect unreachable chunks

## Usage

``` r
dolt_gc(con)
```

## Arguments

- con:

  a \`DoltliteConnection\`.

## Value

A summary string, e.g. \`"12 chunks removed, 45 chunks kept"\`.
