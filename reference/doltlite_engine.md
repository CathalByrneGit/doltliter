# Library information

Library information

## Usage

``` r
doltlite_engine()

doltlite_version()
```

## Value

\`doltlite_engine()\` returns the storage engine name, \`"prolly"\` for
a DoltLite-format database. \`doltlite_version()\` returns the bundled
SQLite version.
