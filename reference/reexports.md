# Objects re-exported from DBI

These are DBI's own functions, re-exported so that they are available
after \`library(doltliter)\` without attaching DBI as well. See the
linked DBI documentation for each.

## Arguments

- ...:

  passed on to the DBI function.

## Value

As documented by DBI.

## See also

\[DBI::Id()\], \[DBI::SQL()\], \[DBI::dbCanConnect()\],
\[DBI::dbIsReadOnly()\], \[DBI::dbListObjects()\],
\[DBI::dbQuoteLiteral()\], \[DBI::dbUnquoteIdentifier()\],
\[DBI::dbWithTransaction()\]
